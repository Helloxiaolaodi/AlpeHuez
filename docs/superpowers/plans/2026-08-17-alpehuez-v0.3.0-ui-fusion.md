# AlpeHuez v0.3.0 UI 融合 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 AFFiNE × Homepage × Homarr 的 UI 设计哲学融入 AlpeHuez v0.3.0：工作台标签移至顶端、状态指示灯、Bento 网格、Glance Bar、块级悬停、Peek View、技师面板 JSON 编辑器。

**Architecture:** 前端全部在单文件 `index.html`（内联 CSS+JS）；后端新增 `status.rs` 后台线程做 HTTP 状态探测（`ureq`），复用现有 `open_internal_page` 子 WebView 机制实现 Peek；开发者面板 `panel/` 加 JSON 编辑器。所有新增链接字段可选、向后兼容。

**Tech Stack:** Tauri v2 (Rust), rusqlite, ureq 2, 原生 JS/CSS

**Spec:** `docs/superpowers/specs/2026-08-17-alpehuez-v0.3.0-ui-fusion-design.md`

## Global Constraints

- 版本号保持 0.3.0，不 bump。
- 所有新增链接字段（`monitor`/`span`/`peek`）可选，旧数据无字段时行为不变。
- 配置驱动：JSON 为唯一数据源，不做拖拽排版。
- 提交规则：commit 只署名 Helloxiaolaodi，不加 Co-Authored-By。
- 全部功能完成后 `tauri build --bundles nsis` 重建，**不立即归档**（等用户确认后再运行 `scripts/release.ps1`）。
- 构建环境：`export PATH="/d/Rust/.cargo/bin:$PATH"`、`RUSTUP_HOME=/d/Rust/.rustup`、`CARGO_HOME=/d/Rust/.cargo`；tauri CLI 在 npm 全局（`tauri build` 而非 `cargo tauri`）。

---

### Task 1: F1 — 工作台标签栏移至顶端

**Files:**
- Modify: `index.html`（CSS `.app-shell` 行 330、HTML 行 2796-3111）

**Interfaces:**
- Consumes: 现有 `#workspaceTabs`（行 2797-2802）、`.action-rail`（2803）、`.tab-sidebar`（2837）、`main.workspace`（2936）、`#velometerDrawer`（3078）
- Produces: `.app-body` 包裹容器；`.app-shell` 变列方向

- [ ] **Step 1: 改 `.app-shell` CSS 为列方向**

在 `index.html` 行 330-336 的 `.app-shell` 规则中加 `flex-direction: column;`：

```css
        .app-shell {
            position: relative;
            z-index: 1;
            display: flex;
            flex-direction: column;
            width: 100vw;
            height: 100vh;
        }
```

- [ ] **Step 2: 加 `.app-body` CSS**

在 `.app-shell` 规则之后（行 336 后）加：

```css
        .app-body {
            display: flex;
            flex: 1;
            min-height: 0;
        }
```

- [ ] **Step 3: 包裹 HTML 结构**

在行 2802（`#workspaceTabs` 的 `</div>`）之后插入 `<div class="app-body">`，在行 3111（app-shell 的 `</div>`）之前插入 `</div>`。结构变为：

```html
    <div class="app-shell">
        <div id="workspaceTabs" class="workspace-tabs" hidden>
            <div id="workspaceTabList" class="workspace-tab-list"></div>
            <button id="workspaceAddBtn" class="workspace-add-btn" type="button" title="新建工作台" aria-label="新建工作台">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M12 5v14"/><path d="M5 12h14"/></svg>
            </button>
        </div>
        <div class="app-body">
            <aside class="action-rail" aria-label="Global actions">
                ...（原样保留）
            </aside>
            <aside class="tab-sidebar" aria-label="Contextual panel">
                ...（原样保留）
            </aside>
            <main class="workspace">
                ...（原样保留）
            </main>
            <div id="velometerDrawer" class="velometer-drawer" hidden>
                ...（原样保留）
            </div>
        </div>
    </div>
```

注意：`#velometerDrawer` 是 `position: absolute`，其包含块仍是 `.app-shell`（`position: relative`），包裹进 `.app-body` 不影响定位。

- [ ] **Step 4: 验证**

启动 App（`tauri dev` 或构建后运行），5 个工作台标签水平显示在窗口顶部（Chrome 标签栏风格），切换正常，左侧 action-rail 和 tab-sidebar 不受影响。

- [ ] **Step 5: 提交**

```bash
git add index.html
git commit -m "feat: move workspace tabs to top bar as full-width strip"
```

---

### Task 2: F2a — status.rs 后端状态探测

**Files:**
- Modify: `src-tauri/Cargo.toml`（加 ureq）
- Create: `src-tauri/src/status.rs`
- Modify: `src-tauri/src/lib.rs`（setup 挂载）

**Interfaces:**
- Consumes: `db::open`、`db::init`、`db::list_workspaces`、`db::get_workspace_links`（db.rs 已有）、`crate::repo_root()`（lib.rs 已有）
- Produces: `status::spawn(app: tauri::AppHandle)`；事件 `service-status-update` 载荷 `{ statuses: [{ url, online, latencyMs }] }`

- [ ] **Step 1: Cargo.toml 加 ureq**

在 `src-tauri/Cargo.toml` 的 `[dependencies]` 中加：

```toml
ureq = "2"
```

- [ ] **Step 2: 创建 status.rs**

创建 `src-tauri/src/status.rs`：

```rust
use std::time::{Duration, Instant};

use serde::Serialize;
use tauri::Emitter;

use crate::db;

#[derive(Serialize, Clone)]
#[serde(rename_all = "camelCase")]
pub struct ServiceStatus {
    pub url: String,
    pub online: bool,
    pub latency_ms: u64,
}

pub fn spawn(app: tauri::AppHandle) {
    std::thread::spawn(move || loop {
        let statuses = check_all(&app);
        let _ = app.emit(
            "service-status-update",
            serde_json::json!({ "statuses": statuses }),
        );
        std::thread::sleep(Duration::from_secs(30));
    });
}

fn check_all(app: &tauri::AppHandle) -> Vec<ServiceStatus> {
    let mut urls: Vec<String> = Vec::new();
    if let Ok(dir) = app.path().app_data_dir() {
        if let Ok(conn) = db::open(&dir.join("alpehuez.db")) {
            if db::init(&conn).is_ok() {
                if let Ok(workspaces) = db::list_workspaces(&conn) {
                    for ws in workspaces {
                        let links = if ws.role == "leader" {
                            std::fs::read_to_string(crate::repo_root().join("links.json")).ok()
                        } else {
                            db::get_workspace_links(&conn, ws.id)
                                .ok()
                                .map(|v| v.to_string())
                        };
                        if let Some(links) = links {
                            urls.extend(collect_monitored_urls(&links));
                        }
                    }
                }
            }
        }
    }
    urls.sort();
    urls.dedup();
    urls.into_iter()
        .map(|url| {
            let (online, latency_ms) = ping(&url);
            ServiceStatus {
                url,
                online,
                latency_ms,
            }
        })
        .collect()
}

fn collect_monitored_urls(links_json: &str) -> Vec<String> {
    let mut urls = Vec::new();
    if let Ok(v) = serde_json::from_str::<serde_json::Value>(links_json) {
        if let Some(icons) = v.get("icons").and_then(|i| i.as_array()) {
            for group in icons {
                if let Some(children) = group.get("children").and_then(|c| c.as_array()) {
                    for item in children {
                        let monitored = item
                            .get("monitor")
                            .and_then(|m| m.as_bool())
                            .unwrap_or(false);
                        if monitored {
                            if let Some(url) = item.get("url").and_then(|u| u.as_str()) {
                                urls.push(url.to_string());
                            }
                        }
                    }
                }
            }
        }
    }
    urls
}

fn ping(url: &str) -> (bool, u64) {
    let start = Instant::now();
    let result = ureq::head(url).timeout(Duration::from_secs(3)).call();
    let latency_ms = start.elapsed().as_millis() as u64;
    match result {
        Ok(_) => (true, latency_ms),
        // 服务器返回任何 HTTP 状态码（含 4xx/5xx）都算在线——服务可达
        Err(ureq::Error::Status(_, _)) => (true, latency_ms),
        Err(_) => (false, latency_ms),
    }
}
```

- [ ] **Step 3: lib.rs 挂载**

在 `src-tauri/src/lib.rs` 行 4 加 `mod status;`，行 66-69 的 setup 中加 `status::spawn(app.handle().clone());`：

```rust
mod commands;
mod db;
mod preview;
mod status;
mod velometer;
```

```rust
        .setup(|app| {
            velometer::spawn(app.handle().clone());
            status::spawn(app.handle().clone());
            Ok(())
        })
```

- [ ] **Step 4: 编译验证**

```bash
export PATH="/d/Rust/.cargo/bin:$PATH" && export RUSTUP_HOME=/d/Rust/.rustup && export CARGO_HOME=/d/Rust/.cargo
cd src-tauri && cargo build 2>&1 | tail -5
```

Expected: 编译通过，无 error。

- [ ] **Step 5: 提交**

```bash
git add src-tauri/Cargo.toml src-tauri/Cargo.lock src-tauri/src/status.rs src-tauri/src/lib.rs
git commit -m "feat: service status probe thread emitting service-status-update every 30s"
```

---

### Task 3: F2b — 状态指示灯前端

**Files:**
- Modify: `index.html`（CSS 加 `.service-dot`、JS `renderContent` 行 4015、`bindEvents` 行 5119 附近）

**Interfaces:**
- Consumes: `service-status-update` 事件（Task 2 产生）
- Produces: `serviceStatuses` Map；卡片右下角状态点

- [ ] **Step 1: 加 CSS**

在 `.nav-card-status.red` 规则（行 1908-1910）之后加：

```css
        .service-dot {
            position: absolute;
            bottom: 10px;
            right: 10px;
            width: 6px;
            height: 6px;
            border-radius: 50%;
            background: var(--text-muted);
            opacity: 0.6;
        }
        .service-dot.online {
            background: var(--success);
            opacity: 1;
            animation: service-breathe 2.4s ease-in-out infinite;
        }
        .service-dot.offline {
            background: var(--danger);
            opacity: 1;
        }
        @keyframes service-breathe {
            0%, 100% { box-shadow: 0 0 0 0 rgba(177, 227, 255, 0.7); }
            50% { box-shadow: 0 0 0 4px rgba(177, 227, 255, 0); }
        }
```

- [ ] **Step 2: 加 serviceStatuses Map**

在 `renderContent` 函数（行 4015）之前加：

```js
        const serviceStatuses = new Map();
```

- [ ] **Step 3: renderContent 加状态点**

在 `renderContent` 的卡片模板（行 4051-4060）中，`statusTitle` 定义之后加：

```js
                        const serviceDot = item.monitor
                            ? `<span class="service-dot ${serviceStatuses.get(item.url) === true ? 'online' : (serviceStatuses.get(item.url) === false ? 'offline' : 'unknown')}" data-url="${escapeHtml(item.url)}" title="Service status"></span>`
                            : '';
```

并在卡片模板中、`<span class="nav-card-status ...">` 之后插入 `${serviceDot}`：

```js
                        return `
                            <button type="button" class="nav-card" data-url="${escapeHtml(item.url)}" data-title="${escapeHtml(item.title)}" title="Open ${escapeHtml(item.title)} inside AlpeHuez">
                                <span class="nav-card-status ${statusClass}" title="${statusTitle}"></span>
                                ${serviceDot}
                                ${iconHtml}
                                <span class="card-copy">
                                    <span class="card-title">${escapeHtml(item.title)}</span>
                                    <span class="card-meta"><span class="card-domain">${escapeHtml(domain)}</span></span>
                                </span>
                            </button>
                        `;
```

- [ ] **Step 4: bindEvents 加监听**

在 `bindEvents` 的 `if (listen) {` 块（行 5119）内、`listen('browser-tab-updated', ...)` 之前加：

```js
                listen('service-status-update', (event) => {
                    const statuses = (event.payload && event.payload.statuses) || [];
                    statuses.forEach((s) => serviceStatuses.set(s.url, s.online));
                    document.querySelectorAll('.service-dot').forEach((dot) => {
                        const online = serviceStatuses.get(dot.dataset.url);
                        dot.className = 'service-dot ' + (online === true ? 'online' : (online === false ? 'offline' : 'unknown'));
                    });
                });
```

- [ ] **Step 5: 验证**

在 `links.json` 中给 2 条链接加 `"monitor": true`（一个可达、一个不可达），启动 App，30s 内卡片右下角出现绿/红状态点，在线点有冰雪蓝呼吸光晕。

- [ ] **Step 6: 提交**

```bash
git add index.html
git commit -m "feat: service status dots on monitored portal cards with breathing glow"
```

---

### Task 4: F3+F4 — Bento 网格 + Glance Bar

**Files:**
- Modify: `index.html`（CSS `.card-grid` 行 1862、`.velometer-pill` 行 231、`:root` 行 13、JS `renderContent` 行 4015）

**Interfaces:**
- Consumes: 链接对象可选 `span: [cols, rows]` 字段
- Produces: `.alpehuez-widget` 类；`--glass-bg`/`--glass-border` 变量；VeloMeter 渐变背景

- [ ] **Step 1: :root 加玻璃变量**

在 `:root`（行 13-31）中加：

```css
            --glass-bg: rgba(255, 255, 255, 0.6);
            --glass-border: rgba(15, 23, 42, 0.08);
```

在 `html[data-theme="space"]`（行 33-48）中加：

```css
            --glass-bg: rgba(30, 41, 59, 0.62);
            --glass-border: rgba(255, 255, 255, 0.08);
```

- [ ] **Step 2: card-grid 改 280px**

`.card-grid` 规则（行 1862-1868）中 `minmax(210px, 1fr)` 改为 `minmax(280px, 1fr)`。

- [ ] **Step 3: 加 .alpehuez-widget CSS**

在 `.nav-card:hover` 规则（行 1888-1892）之后加：

```css
        .nav-card.alpehuez-widget {
            background: var(--glass-bg);
            backdrop-filter: blur(16px);
            -webkit-backdrop-filter: blur(16px);
            border: 1px solid var(--glass-border);
            border-radius: 12px;
        }
        .nav-card.alpehuez-widget:hover {
            border-color: #FFE0AE;
            box-shadow: 0 16px 32px -14px rgba(255, 224, 174, 0.35), var(--shadow);
        }
```

- [ ] **Step 4: renderContent 加 span + widget 类**

在 `renderContent` 卡片模板中，`statusTitle` 定义之后加：

```js
                        const spanStyle = Array.isArray(item.span)
                            ? `style="grid-column: span ${Math.max(1, item.span[0] | 0)}; grid-row: span ${Math.max(1, item.span[1] | 0)};"`
                            : '';
```

卡片 `<button>` 的 class 改为 `class="nav-card alpehuez-widget"`，并加 `${spanStyle}`：

```js
                        return `
                            <button type="button" class="nav-card alpehuez-widget" data-url="${escapeHtml(item.url)}" data-title="${escapeHtml(item.title)}" title="Open ${escapeHtml(item.title)} inside AlpeHuez" ${spanStyle}>
                                <span class="nav-card-status ${statusClass}" title="${statusTitle}"></span>
                                ${serviceDot}
                                ${iconHtml}
                                <span class="card-copy">
                                    <span class="card-title">${escapeHtml(item.title)}</span>
                                    <span class="card-meta"><span class="card-domain">${escapeHtml(domain)}</span></span>
                                </span>
                            </button>
                        `;
```

- [ ] **Step 5: Glance Bar — velometer-pill 渐变**

`.velometer-pill` 规则（行 231-240）中 `background: var(--bg-card);` 改为：

```css
            background: linear-gradient(135deg, rgba(177, 227, 255, 0.45), rgba(255, 224, 174, 0.45));
```

- [ ] **Step 6: 验证**

给一条链接加 `"span": [2,1]`，渲染为宽卡片；VeloMeter 胶囊显示冰雪蓝→暖麦色渐变背景。

- [ ] **Step 7: 提交**

```bash
git add index.html
git commit -m "feat: bento grid spans and glass widget cards, velometer glance bar gradient"
```

---

### Task 5: F5 — 块级悬停（AFFiNE 借鉴）

**Files:**
- Modify: `index.html`（CSS、JS `renderFileTree` 行 4213、`renderPortalSidebar` 行 3934、`bindEvents` 行 4889 附近）

**Interfaces:**
- Consumes: 现有 `renderFileTree`、`renderPortalSidebar`、`openExternal`（行 3830）
- Produces: `.file-item`/`.file-actions`/`.file-action`、`.portal-category-wrap`/`.portal-category-actions` 结构

- [ ] **Step 1: 加 CSS**

在 `.file-link` 相关 CSS 附近加：

```css
        .file-item {
            position: relative;
        }
        .file-actions {
            position: absolute;
            right: 6px;
            top: 50%;
            transform: translateY(-50%);
            display: flex;
            gap: 2px;
            opacity: 0;
            transition: opacity 0.15s ease;
        }
        .file-item:hover .file-actions { opacity: 1; }
        .file-action {
            width: 22px;
            height: 22px;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            border: none;
            border-radius: 6px;
            background: var(--bg-card);
            color: var(--text-muted);
            cursor: pointer;
        }
        .file-action:hover { color: var(--text-main); }
        .portal-category-wrap {
            position: relative;
        }
        .portal-category-actions {
            position: absolute;
            right: 6px;
            top: 50%;
            transform: translateY(-50%);
            display: flex;
            gap: 2px;
            opacity: 0;
            transition: opacity 0.15s ease;
        }
        .portal-category-wrap:hover .portal-category-actions { opacity: 1; }
```

- [ ] **Step 2: renderFileTree 加 hover 操作**

在 `renderFileTree`（行 4213）的 `filesHtml` 模板中，把 `.file-link` 按钮包进 `.file-item` 容器并加操作按钮：

```js
                const filesHtml = (folder.files || []).map((file) => {
                    const fileName = file.name || 'Untitled';
                    const relative = encodeURI(file.url || fileName);
                    const fileUrl = `${base}${encodeURIComponent(folder.slug)}/${relative}`;
                    return `
                        <div class="file-item">
                            <button type="button" class="file-link" draggable="true" data-url="${escapeHtml(fileUrl)}" data-title="${escapeHtml(fileName)}">
                                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><path d="M14 2v6h6"/></svg>
                                <span>${escapeHtml(fileName)}</span>
                            </button>
                            <span class="file-actions">
                                <button type="button" class="file-action" data-action="copy" data-url="${escapeHtml(fileUrl)}" title="Copy path" aria-label="Copy path">
                                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="9" y="9" width="13" height="13" rx="2"/><path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/></svg>
                                </button>
                                <button type="button" class="file-action" data-action="open" data-url="${escapeHtml(fileUrl)}" title="Open external" aria-label="Open external">
                                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6"/><path d="M15 3h6v6"/><path d="M10 14 21 3"/></svg>
                                </button>
                            </span>
                        </div>
                    `;
                }).join('');
```

- [ ] **Step 3: renderPortalSidebar 加 hover 操作**

在 `renderPortalSidebar`（行 3934）的 category 模板中，把 `.portal-category-link` 包进 `.portal-category-wrap` 并加复制按钮：

```js
                return `
                    <div class="portal-category-wrap">
                        <button type="button" class="portal-category-link${activeClass}" data-target="main-group-${index}">
                            <span class="portal-category-name">${escapeHtml(section.mainTitle)}</span>
                            <span class="file-count">${itemCount}</span>
                        </button>
                        <span class="portal-category-actions">
                            <button type="button" class="file-action" data-action="copy" data-copy="${escapeHtml(section.mainTitle)}" title="Copy name" aria-label="Copy name">
                                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="9" y="9" width="13" height="13" rx="2"/><path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/></svg>
                            </button>
                        </span>
                    </div>
                `;
```

- [ ] **Step 4: bindEvents 加操作处理**

在 `bindEvents` 中 `$('fileTree').addEventListener('click', ...)`（行 4889）之前加：

```js
            document.addEventListener('click', (event) => {
                const action = event.target.closest('.file-action');
                if (!action) return;
                event.stopPropagation();
                const url = action.dataset.url;
                const copyText = action.dataset.copy;
                if (action.dataset.action === 'copy') {
                    const text = copyText || url || '';
                    if (navigator.clipboard && navigator.clipboard.writeText) {
                        navigator.clipboard.writeText(text).catch(() => {});
                    }
                } else if (action.dataset.action === 'open' && url) {
                    openExternal(url);
                }
            });
```

- [ ] **Step 5: 验证**

鼠标悬停文件树项，右侧浮现复制/打开图标；悬停 Portal 分类项，浮现复制图标。

- [ ] **Step 6: 提交**

```bash
git add index.html
git commit -m "feat: block hover actions on file tree and portal categories"
```

---

### Task 6: F6 — Peek View（补给递送）

**Files:**
- Modify: `src-tauri/src/commands.rs`（`open_internal_page` 行 900、`open_internal_page_impl` 行 912）
- Modify: `index.html`（CSS、HTML、JS `openLink` 行 3823、`bindEvents` 行 5119）

**Interfaces:**
- Consumes: `open_internal_page`（加 `peek` 参数）、`close_internal_page`、`getStageBounds`（行 3742）
- Produces: `openPeek(url, title)`、`closePeek()`、`peekLabel` 变量、`#peekPanel` 容器

- [ ] **Step 1: commands.rs 加 peek 参数**

`open_internal_page`（行 900-910）加 `peek: Option<bool>` 参数并传给 impl：

```rust
#[tauri::command]
pub async fn open_internal_page(
    app: tauri::AppHandle,
    url: String,
    title: Option<String>,
    x: Option<f64>,
    y: Option<f64>,
    width: Option<f64>,
    height: Option<f64>,
    peek: Option<bool>,
) -> Result<InternalPageInfo, String> {
    open_internal_page_impl(app, url, title, x, y, width, height, peek)
}
```

`open_internal_page_impl`（行 912-920）签名加 `peek: Option<bool>`，行 928-932 的 label 生成改为：

```rust
    let prefix = if peek.unwrap_or(false) { "peek-" } else { "browser-" };
    let label = format!("{prefix}{hash}");
```

注意：`layout_internal_pages`（行 1074）和 `activate_internal_page`（行 1053）已按 `browser-` 前缀过滤，`peek-` 标签自动被跳过，无需改动。

- [ ] **Step 2: 加 peek CSS**

在 `.velometer-drawer` 相关 CSS 附近加：

```css
        .peek-panel {
            position: absolute;
            right: 0;
            top: 56px;
            width: 400px;
            z-index: 35;
            pointer-events: none;
        }
        .peek-panel[hidden] { display: none; }
        .peek-head {
            height: 36px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 8px;
            padding: 0 8px 0 14px;
            background: var(--panel);
            backdrop-filter: blur(16px);
            -webkit-backdrop-filter: blur(16px);
            border-left: 1px solid var(--line-strong);
            border-bottom: 1px solid var(--line-strong);
            border-radius: 12px 0 0 0;
            pointer-events: auto;
        }
        .peek-title {
            flex: 1;
            min-width: 0;
            overflow: hidden;
            text-overflow: ellipsis;
            white-space: nowrap;
            font-size: 12px;
            color: var(--text-muted);
        }
```

- [ ] **Step 3: 加 peek HTML**

在 `main.workspace` 内、`<header class="topbar">`（行 2937）之前插入：

```html
            <div id="peekPanel" class="peek-panel" hidden>
                <div class="peek-head">
                    <span id="peekTitle" class="peek-title">Peek</span>
                    <button id="peekCloseBtn" class="icon-btn" type="button" title="Close peek" aria-label="Close peek">&times;</button>
                </div>
            </div>
```

同时给 `.workspace` 规则（行 1245-1252）加 `position: relative;`。

- [ ] **Step 4: 加 peek JS**

在 `openLink`（行 3823）之前加：

```js
        let peekLabel = null;

        async function openPeek(url, title) {
            if (!isDesktop) {
                window.open(url, '_blank');
                return;
            }
            if (peekLabel) {
                try { await invoke('close_internal_page', { label: peekLabel }); } catch (error) {}
                peekLabel = null;
            }
            const bounds = getStageBounds();
            const peekWidth = 400;
            const headHeight = 36;
            const info = await invoke('open_internal_page', {
                url,
                title,
                x: bounds.x + bounds.width - peekWidth,
                y: bounds.y + headHeight,
                width: peekWidth,
                height: Math.max(0, bounds.height - headHeight),
                peek: true
            });
            peekLabel = info.label;
            $('peekTitle').textContent = info.title || title;
            $('peekPanel').hidden = false;
        }

        async function closePeek() {
            if (peekLabel) {
                try { await invoke('close_internal_page', { label: peekLabel }); } catch (error) {}
                peekLabel = null;
            }
            $('peekPanel').hidden = true;
        }
```

- [ ] **Step 5: openLink 支持 peek**

`openLink`（行 3823-3828）改为：

```js
        async function openLink(item, event) {
            trackClick(item);
            if (item.peek || (event && (event.ctrlKey || event.metaKey))) {
                await openPeek(item.url, item.title);
            } else {
                await openUrl(item.url, item.title);
            }
            renderContent();
            renderBadges();
        }
```

- [ ] **Step 6: 点击处理器传 event**

`$('content').addEventListener('click', ...)`（行 4976-4981）中 `openLink(item)` 改为 `openLink(item, event)`。

- [ ] **Step 7: bindEvents 处理 peek 标签**

在 `bindEvents` 的 `if (listen) {` 块中，`listen('browser-tab-updated', ...)`（行 5120）改为：

```js
                listen('browser-tab-updated', (event) => {
                    const info = event.payload || {};
                    if (info.label === peekLabel) {
                        $('peekTitle').textContent = info.title || info.url;
                        return;
                    }
                    const isNew = !browserTabs.has(info.label);
                    browserTabs.set(info.label, { label: info.label, title: info.title || info.url, url: info.url || '' });
                    renderBrowserTabs();
                    if (isNew && info.label) activateBrowserTab(info.label);
                });
```

`listen('browser-tab-closed', ...)`（行 5127）改为：

```js
                listen('browser-tab-closed', (event) => {
                    const label = typeof event.payload === 'string' ? event.payload : (event.payload && event.payload.label);
                    if (label === peekLabel) {
                        peekLabel = null;
                        $('peekPanel').hidden = true;
                    }
                    if (label && browserTabs.has(label)) {
                        browserTabs.delete(label);
                        renderBrowserTabs();
                    }
                });
```

- [ ] **Step 8: 绑定 peek 关闭按钮**

在 `bindEvents` 中加：

```js
            const peekCloseBtn = $('peekCloseBtn');
            if (peekCloseBtn) peekCloseBtn.addEventListener('click', closePeek);
```

- [ ] **Step 9: 验证**

Ctrl+点击任意链接，右侧滑出 400px 毛玻璃面板加载网页；点关闭按钮面板消失；打开普通标签不受影响。

- [ ] **Step 10: 提交**

```bash
git add src-tauri/src/commands.rs index.html
git commit -m "feat: peek view opens narrow right-side webview on ctrl-click or peek links"
```

---

### Task 7: F7 — 技师面板 JSON 编辑器

**Files:**
- Modify: `panel/panel.js`（`fieldHtml` 行 280、`renderWorkspaces` 行 773）
- Modify: `panel/panel.css`（textarea 高度）

**Interfaces:**
- Consumes: `get_workspace_links`、`save_workspace_links` 命令（commands.rs 已有）
- Produces: `editWorkspaceLinks(id)` 函数；每个工作台卡片加"链接 JSON"按钮

- [ ] **Step 1: fieldHtml 支持 rows**

`fieldHtml`（行 280-293）中 textarea 的 `extra` 改为支持 `rows`：

```js
  const extra = f.type === 'textarea' ? `rows="${f.rows || 3}"` : `type="${typeAttr}"`;
```

- [ ] **Step 2: renderWorkspaces 加按钮**

`renderWorkspaces`（行 773-820）的 `.ws-card-actions` 中、编辑按钮之后加：

```js
        <button type="button" class="btn" data-links="${w.id}">链接 JSON</button>
```

并在 `list.querySelectorAll('[data-edit]')` 绑定之后加：

```js
  list.querySelectorAll('[data-links]').forEach((btn) => {
    btn.addEventListener('click', () => editWorkspaceLinks(Number(btn.dataset.links)));
  });
```

- [ ] **Step 3: 加 editWorkspaceLinks 函数**

在 `editWorkspace` 函数（行 822-834）之后加：

```js
async function editWorkspaceLinks(id) {
  const { invoke } = window.__TAURI__.core;
  let data;
  try {
    data = await invoke('get_workspace_links', { id });
  } catch (e) {
    toast('读取链接失败: ' + e.message, 'error');
    return;
  }
  const text = JSON.stringify(data, null, 2);
  openModal('工作台链接 JSON', [
    { key: 'json', label: 'JSON', type: 'textarea', rows: 18, value: '' }
  ], async (values) => {
    let parsed;
    try {
      parsed = JSON.parse(values.json);
    } catch (e) {
      toast('JSON 解析失败: ' + e.message, 'error');
      return;
    }
    try {
      await invoke('save_workspace_links', { id, data: parsed });
      toast('已保存');
    } catch (e) {
      toast('保存失败: ' + e.message, 'error');
    }
  });
  const ta = document.getElementById('f_json');
  if (ta) ta.value = text;
}
```

注意：`openModal` 的 `fieldHtml` 用 `value` 属性填充，大 JSON 含换行会转义异常，所以先传空 `value`，再在 `openModal` 之后用 JS 直接设置 `ta.value = text`。

- [ ] **Step 4: panel.css 加 textarea 高度**

在 `panel/panel.css` 中加：

```css
#f_json {
  font-family: ui-monospace, SFMono-Regular, Consolas, monospace;
  font-size: 12px;
  line-height: 1.5;
  white-space: pre;
  resize: vertical;
}
```

- [ ] **Step 5: 验证**

打开开发者面板 → 工作台 tab → 点某副将的"链接 JSON"按钮，弹出 textarea 显示格式化 JSON；修改后保存，Portal 重新渲染生效。

- [ ] **Step 6: 提交**

```bash
git add panel/panel.js panel/panel.css
git commit -m "feat: workspace links JSON editor in developer panel"
```

---

### Task 8: 重建发布（不归档）

**Files:**
- 无源码改动

- [ ] **Step 1: 全量构建**

```bash
export PATH="/d/Rust/.cargo/bin:$PATH" && export RUSTUP_HOME=/d/Rust/.rustup && export CARGO_HOME=/d/Rust/.cargo
cd /d/YL2026/sun-panel/my-nav/my-nav && tauri build --bundles nsis 2>&1 | tail -10
```

Expected: `Finished release profile` + `Built application at: ...\my-nav-panel.exe` + `Finished 1 bundle at: ...\AlpeHuez_0.3.0_x64-setup.exe`。

- [ ] **Step 2: 验证产物**

确认 `src-tauri/target/release/my-nav-panel.exe` 和 `src-tauri/target/release/bundle/nsis/AlpeHuez_0.3.0_x64-setup.exe` 存在且时间戳为本次构建。

- [ ] **Step 3: 提交源码改动**

```bash
git add -A
git commit -m "feat: alpehuez v0.3.0 ui fusion (top tabs, status dots, bento grid, glance bar, block hover, peek view, json editor)"
```

- [ ] **Step 4: 不归档**

**不要**运行 `scripts/release.ps1`。等用户确认后再归档到 `releases/v0.3.0/`。

---

## 自检记录

- **Spec 覆盖**：F1→Task 1、F2→Task 2+3、F3→Task 4、F4→Task 4、F5→Task 5、F6→Task 6、F7→Task 7、发布要求→Task 8。全部 spec 功能有对应任务。
- **占位符扫描**：无 TBD/TODO；所有代码步骤含完整代码。
- **类型一致性**：`service-status-update` 载荷 `{ statuses: [{ url, online, latencyMs }] }` 在 Task 2（Rust 端 `ServiceStatus` camelCase）与 Task 3（JS 端 `s.url`/`s.online`）一致；`open_internal_page` 的 `peek` 参数在 Task 6 的 Rust 签名与 JS invoke 一致；`monitor`/`span`/`peek` 字段在 Task 3/4/6 的 JS 读取与 spec 数据模型一致。
- **已知风险**：`ureq::head` 对不支持 HEAD 的站点可能返回 405，但 `Err(Error::Status(_, _))` 仍算在线（服务可达），符合"服务是否在线"语义。
