# AlpeHuez v0.4.0 未完成任务清单

> 日期：2026-08-18
> 状态快照：消息 L（L1–L6 UI 升维：侧边栏幽灵高亮 / Timeline / Heatmap / Quick Actions dock / VeloMeter 质感 / 进程列表）已全部完成并 review 通过；消息 G（加载页自行车 + 侧边栏文字可见）已验证在案。剩余任务为计划收尾 T30–T33 与发布流程。

---

## 待办任务（按顺序执行）

### T30: WebView 标签休眠（前端）

- **Files:** `index.html`（`renderBrowserTabs`、`activateBrowserTab`、新增休眠定时器）
- **Consumes:** `set_internal_page_visible`（Task 3，Rust 命令已就绪）
- **Produces:** 标签对象加 `sleeping: boolean`；`sleepTab(label)` / `wakeTab(label)`；休眠标签 Zzz 样式
- **行为:** 非活跃标签休眠后调用 Rust 隐藏 webview，前台标签唤醒
- **状态:** pending

### T31: 后台启动（启动即隐藏 + Alt+A 呼出）

- **Files:** `src-tauri/tauri.conf.json`（主窗口加 `"visible": false`）、`src-tauri/src/commands.rs`（新增 `mark_first_run`）、`src-tauri/src/lib.rs`（首次运行显示窗口）、`src-tauri/build.rs`、`src-tauri/capabilities/`（权限）、`index.html`（首次运行提示 toast）
- **行为:** 首次运行显示窗口 + 提示，之后启动即驻留后台，Alt+A 呼出
- **状态:** pending

### T32: 前端版本号 v0.4.0

- **Files:** `index.html`（`<title>`、`aboutVersion`、`aboutVersionMeta`、`fallbackVersion`、`renderContextAbout`）、`releases/latest.json`、`README.md`
- **行为:** 所有 `0.3.0` → `0.4.0`
- **状态:** pending

### T33: 构建发布（安装包 + APK）

- **Files:** 新建 `releases/v0.4.0/` 目录
- **Produces:** `AlpeHuez_0.4.0_x64-setup.exe`（`cargo tauri build`，nsis）+ `AlpeHuez_0.4.0_arm64.apk`（Android 流程）+ `SHA256SUMS.txt` + `releases/latest.json` 更新指向 v0.4.0
- **验证:** 安装 exe 后确认 About 显示 v0.4.0、无幽灵终端、F11 正常、无边框窗口正常（新 exe 带 `decorations:false`，与双标题栏修复后的前端一致）
- **状态:** pending

---

## 收尾流程

1. **全分支最终代码审查**（final whole-branch review）
2. 有 findings → 一次 fix dispatch + scoped re-review
3. 通过后清理 SDD 工作区，执行 finishing-a-development-branch
4. `git commit -m "release: AlpeHuez v0.4.0 (rebuilt installer + android arm64 apk)"`

---

## 已搁置 / 遗留事项（deferred MINORs）

### 视觉验证类（需用户本地 `node panel/server.mjs` 目检）

- 深色模式整体渲染（薰衣草底 / 卡片 / 环形图底槽+辉光）——T13c/T13d/T13e 遗留
- Telemetry Pod 抽屉滑出 / 点击外部关闭——T15
- VeloMeter 胶囊双环 / 抽屉大环渐变辉光 / 进程列表新样式——T16/L6
- 堆叠 Toast 动画——T17
- 右键菜单玻璃质感——T18
- 空态线框自行车——T19
- 按钮回弹 / 鼠标跟随辉光——T20
- 标题栏 / 双栏去重效果——T29/双栏修复（9d302d8）

### 功能验证类（需用户本地实测）

- 消息 G：加载页自行车 + 侧边栏文字可见——**已实现（commit 438ac62）**，如仍有异常请反馈复测
- HF 同步端到端（真实备份到 HF dataset 仓库，需 token + VPN）
- F11 专注模式进出
- My Files 文件夹点击不跳回 Portal
- Climber 驻留计时（网页停留 >1 分钟得分增长）
- Telemetry 设置：Zen 开关 / Clear 清空 / dock 按钮联动
- 双标题栏：旧 exe 只显示原生栏；新版无边框 exe 只显示自定义栏

### 代码搁置项（非阻塞）

- `readTimeline` 不强制 `e.t` 为 Number——外部损坏数据下显示 "NaN d ago"，真实数据不可达（L2）
- Settings 的 zenToggle 不反映 dock 按钮实时状态变更——pre-existing（T28/L4）
- `winreg` 依赖可能未使用（T4）
- `open_url_scheme` 使用全限定 `std::process::Command::new`——纯风格（T1）
