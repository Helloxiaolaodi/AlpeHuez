# AlpeHuez v0.4.0 未完成任务清单

> 日期：2026-08-18
> 状态快照：消息 L（L1–L6 UI 升维：侧边栏幽灵高亮 / Timeline / Heatmap / Quick Actions dock / VeloMeter 质感 / 进程列表）已全部完成并 review 通过；消息 G（加载页自行车 + 侧边栏文字可见）已验证在案。**计划收尾 T30–T33 已全部完成并 review/验证通过，v0.4.0 发布已提交（1e7cacc）**。剩余为全分支最终代码审查 + 收尾流程。

---

## 待办任务（按顺序执行）

### ~~T30: WebView 标签休眠（前端）~~ ✅ 已完成

- **提交:** `a985485`（review 双 PASS）
- **内容:** `sleepTab(label)` / `wakeTab(label)`（调用 `set_internal_page_visible` 隐藏/显示 webview，try/catch 包裹）；`renderBrowserTabs` 休眠态渲染（`.browser-tab.sleeping` class、icon "Zzz"、标题 "Zzz… click to wake"）；CSS `.browser-tab.sleeping { opacity: 0.55; filter: blur(0.3px); }`；`activateBrowserTab` 唤醒休眠标签 + 更新 `lastActive`；60s 定时器检查非活跃标签（15 分钟阈值）自动休眠
- **待用户验证:** 本地 `cargo tauri dev` 打开多个网页标签，等待 15 分钟（或临时把阈值改小）观察非活跃标签显示 Zzz 休眠，点击后唤醒

### ~~T31: 后台启动（启动即隐藏 + Alt+A 呼出）~~ ✅ 已完成

- **提交:** `1ceb56e`（review 双 PASS）
- **内容:** 主窗口 `"visible": false`（启动即隐藏驻留后台）；Rust 新增 `mark_first_run` 命令（`app_config_dir()/first_run.marker` 标记）+ lib.rs setup 首次运行显示窗口（Android 恒显示——无 Alt+A 可唤回；桌面仅首启显示）；`allow-mark-first-run` 权限加入 nav-site.json + default.json；前端首次运行英文 toast（DOMContentLoaded 内绑定）；Alt+A 快捷键沿用既有实现
- **待用户验证:** 首次启动显示窗口 + 提示 toast；关闭后再次启动驻留后台，Alt+A 呼出/隐藏

### ~~T32: 前端版本号 v0.4.0~~ ✅ 已完成

- **提交:** `1b721870`（review 双 PASS）
- **内容:** `index.html` 8 处 `0.3.0` → `0.4.0`（title / contextAboutVersion / versionChip / aboutVersion / Update Notes / aboutVersionMeta / renderContextAbout fallback / fallbackVersion）；`releases/latest.json` 3 处（latest + 2 个产物文件名）；README.md 已核实无版本引用未改；Rust 侧已在 T5 升到 0.4.0

### ~~T33: 构建发布（安装包 + APK）~~ ✅ 已完成

- **提交:** `1e7cacc`（controller 直接执行，验证通过）
- **内容:** `cargo tauri build --bundles nsis` → `AlpeHuez_0.4.0_x64-setup.exe`（4140824 B）+ portable `my-nav-panel.exe`（16950784 B，FileVersion 0.4.0）；Android 流程 → `AlpeHuez_0.4.0_arm64.apk`（4679244 B，versionName 0.4.0/versionCode 4000，apksigner verify 通过，debug 证书与 v0.3.0 一致）；`SHA256SUMS.txt` 3 条（sha256sum 复核一致）；`releases/latest.json` 更新 latest=0.4.0 + 3 产物；`releases/README.md` 加 0.4.0 行
- **待用户验证:** 安装 exe 确认 About 显示 v0.4.0、无幽灵终端、F11 正常、无边框窗口正常（新 exe 带 `decorations:false`）；APK 真机安装验证

---

## 收尾流程

1. **全分支最终代码审查**（final whole-branch review）——**进行中**
2. 有 findings → 一次 fix dispatch + scoped re-review
3. 通过后清理 SDD 工作区，执行 finishing-a-development-branch
4. ~~`git commit -m "release: AlpeHuez v0.4.0 (rebuilt installer + android arm64 apk)"`~~ ✅ 已完成（`1e7cacc`）

---

## 已搁置 / 遗留事项（deferred MINORs）

### 视觉验证类（需用户本地 `node panel/server.mjs` 目检）

- 深色模式整体渲染（薰衣草底 / 卡片 / 环形图底槽+辉光）——T13c/T13d/T13e 遗留
- Telemetry Pod 抽屉滑出 / 点击外部关闭——T15
- VeloMeter 胶囊双环 / 抽屉大环渐变辉光 / 进程列表新样式——T16/L6
- 堆叠 Toast 动画——T17
- 右键菜单玻璃质感——T18
- 空态线框自行车——T19
- 按钮回弹 / 鼠标跟随辉光——T20
- 标题栏 / 双栏去重效果——T29/双栏修复（9d302d8）

### 功能验证类（需用户本地实测）

- 消息 G：加载页自行车 + 侧边栏文字可见——**已实现（commit 438ac62）**，如仍有异常请反馈复测
- T30：WebView 标签休眠——**已实现（commit a985485）**，15 分钟休眠/点击唤醒需本地 `cargo tauri dev` 实测
- T31：后台启动——**已实现（commit 1ceb56e）**，首次启动显示窗口 + 提示 toast、二次启动驻留后台 + Alt+A 呼出需本地实测
- T33：v0.4.0 发布——**已实现（commit 1e7cacc）**，安装 `releases/v0.4.0/AlpeHuez_0.4.0_x64-setup.exe` 后确认 About 显示 v0.4.0、无幽灵终端、F11 进出、无边框窗口正常；`AlpeHuez_0.4.0_arm64.apk` 真机安装验证
- HF 同步端到端（真实备份到 HF dataset 仓库，需 token + VPN）
- F11 专注模式进出
- My Files 文件夹点击不跳回 Portal
- Climber 驻留计时（网页停留 >1 分钟得分增长）
- Telemetry 设置：Zen 开关 / Clear 清空 / dock 按钮联动
- 双标题栏：旧 exe 只显示原生栏；新版无边框 exe 只显示自定义栏

### 代码搁置项（非阻塞）

- `readTimeline` 不强制 `e.t` 为 Number——外部损坏数据下显示 "NaN d ago"，真实数据不可达（L2）
- Settings 的 zenToggle 不反映 dock 按钮实时状态变更——pre-existing（T28/L4）
- `winreg` 依赖可能未使用（T4）
- `open_url_scheme` 使用全限定 `std::process::Command::new`——纯风格（T1）
